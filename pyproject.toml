[project]
name = "tac-master"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
    "pydantic",
    "requests",
    "boto3",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]
